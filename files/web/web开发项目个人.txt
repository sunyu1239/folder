web项目
身份验证过了，才能才做数据库里的数据。所以每次都要输入用户名和密码，做登陆的动作来验证身份，验证过了，就能通过页面的逻辑网页，间接的操作数据库中的个人数据。身份验证有很多种方式，最原始的就是根据页面上输入的用户和密码，匹配数据库中查找的密码是否一样。

浏览器和服务器交互会产生一些有意义的全局数据，临时用到的放在session里，永久数据存到数据库里面。

session身份验证被token身份验证取代了。